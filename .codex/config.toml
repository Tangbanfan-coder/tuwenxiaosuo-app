[agents]
# Concurrency ceiling, not a target. Dispatch fewer workers when ownership or
# shared contracts overlap; reserve parallel slots for independent file sets.
max_threads = 3

# Workers may not create more workers. The orchestrator remains responsible
# for integration, review, and user-facing decisions.
max_depth = 1
