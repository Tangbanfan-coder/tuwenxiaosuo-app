# 变更日志索引

本文件只保存日志规范和月份索引，不保存完整变更正文。当前代码、测试和运行结果是当前行为的首要事实来源；历史日志用于追溯原因，不能替代对现状的验证。

## 读取与记录规则

- 默认不要全文读取月度日志。先查看本索引和目标月份的标题列表，再按模块名、文件名、符号、错误信息或日期使用 `rg` 定向搜索。
- 新记录写入 `logs/YYYY-MM.md`，按时间倒序放在文件顶部，包含目标、关键改动、原因、验证结果和残余风险。
- 长期有效的模块边界、依赖方向和数据流应写入架构文档；重要架构取舍写入 ADR；面向用户的版本变化写入 `CHANGELOG.md`。
- 单月日志异常庞大时可拆为 `YYYY-MM-topic.md`，但必须保留本索引入口，不改写历史结论。

## 月份索引

- [2026-08](logs/2026-08.md)：应用功能、写作上下文、长对话性能、图片链路、Android 发布、界面调整、职责拆分与开发规范。
